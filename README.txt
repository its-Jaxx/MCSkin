Double-click the requirements.bat file.
Alternatively, open command prompt, navigate to correct folder and run "pip install -r requirements.txt"
Don't forget to add your bot token at the bottom of the code
